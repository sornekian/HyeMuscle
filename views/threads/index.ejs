<%- include('../partials/header') %>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
    </tr>
  </thead>
  
  <tbody>
    <% threads.forEach(function(t) { %>
        <tr>
          <td><%= t.title %></td>
          <td><%= t.content %></td>
          <td><a href="/threads/<%= t._id %>">View Post</a></td>
        </tr>
        <% }); %>

  </tbody>
</table>

<%- include('../partials/footer') %>